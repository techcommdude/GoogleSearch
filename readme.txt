This project has been deployed on Heroku as a static project.

-Index.php is required for static HTML.
-You need to connect to Heroku through VSCode and then connect to the remote Heroku repository in order to push changes.